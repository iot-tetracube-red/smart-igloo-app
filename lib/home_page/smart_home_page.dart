import 'package:flutter/material.dart';

class SmartHomePage extends StatelessWidget {
  const SmartHomePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text("Your house");
  }
}
