import 'package:flutter/material.dart';

class JoinSmartIglooHubPage extends StatelessWidget {
  const JoinSmartIglooHubPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text("settings page");
  }
}
